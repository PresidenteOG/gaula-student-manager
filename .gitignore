# ── Backend (Java / Maven) ──────────────────────────────
Backend/target/
Backend/*.log
Backend/logs/
Backend/data/
Backend/.mvn/wrapper/maven-wrapper.jar
# local dev credentials — never committed
Backend/src/main/resources/application-local.properties

# ── Frontend (Flutter) ──────────────────────────────────
Frontend/build/
Frontend/.dart_tool/
Frontend/.flutter-plugins
Frontend/.flutter-plugins-dependencies
Frontend/.pub-cache/
Frontend/coverage/
# env files: dev.json + prod.json ship as templates (no secrets, just API URLs).
# The Pages workflow overrides prod.json from the API_BASE_URL repo variable.
Frontend/env/staging.json
Frontend/env/lan.json
# Flutter Android signing
Frontend/android/key.properties
Frontend/android/app/google-services.json
**/*.keystore
**/*.jks

# ── IDE / OS ────────────────────────────────────────────
.idea/
.vscode/
*.iml
.settings/
.classpath
.project
.DS_Store
Thumbs.db

# ── Local tooling / scratch ─────────────────────────────
.claude/
.mcp.json
graphify-out/
apache-maven-*/
*.bat

# nested Gradle build dirs (Flutter android/ios)
**/build/
